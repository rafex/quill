# SpecNative — venv is generated; do not commit
.specnative/.venv/

# Rust
/target/
**/*.sqlite
**/*.sqlite-wal
**/*.sqlite-shm

# downloaded ONNX embedding models (fetched via `search-service download-model`)
**/models/

# Frontend (web/) — generated artifacts
web/node_modules/
web/dist/
web/wasm/pkg/
